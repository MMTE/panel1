{
  "name": "@panel1/mod-catalog",
  "version": "0.1.0",
  "description": "Panel1 catalog module (products, components, billing plans, legacy plans)",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts"
  },
  "scripts": {
    "build": "echo \"@panel1/mod-catalog: skip isolated tsc (CatalogService imports host runtime; typecheck via apps/api + tsx)\"",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@panel1/types": "workspace:*",
    "@panel1/core": "workspace:*",
    "drizzle-orm": "^0.29.3",
    "hono": "^4.7.5",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^5.5.3"
  }
}
