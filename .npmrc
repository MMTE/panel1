# Auto-install peer dependencies and enforce they match
auto-install-peers=true
strict-peer-dependencies=true

# Use pnpm's strict isolated node_modules layout (not shamefully-hoist).
# This surfaces phantom dependencies so they can be fixed at the source.
node-linker=isolated

# Global content-addressed store — avoids re-downloading packages across projects
store-dir=~/.pnpm-store
